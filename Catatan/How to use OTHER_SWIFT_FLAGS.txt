import SwiftUI

struct ConfigCheck {
    static func printConfig() {
        
        // 1. Cek Environment
        #if STAGING
        print("🌍 Environment: STAGING (Pakai Server Dev)")
        let baseURL = "https://api-staging.kompas.id"
        #elseif PRODUCTION
        print("🌍 Environment: PRODUCTION (Pakai Server Live)")
        let baseURL = "https://api.kompas.id"
        #endif
        
        // 2. Cek Mode Debugging
        #if DEBUG
        print("🐞 Mode: DEBUG (Log aktif, Crashlytics dimatikan)")
        // enableLogging()
        #else
        print("🚀 Mode: RELEASE (Log mati, Crashlytics aktif)")
        // disableLogging()
        #endif
    }
}