############
# Configs
############

configs:
  Staging Debug: debug
  Production Debug: debug
  Staging Release: release
  Production Release: release
  
targets:

  ############
  # Framework
  ############
  
  XAuthKit:
    name: XAuthKit
    type: framework
    platform: iOS

    settings:
      INFOPLIST_FILE: XAuthKit/Configs/Info.plist
      DEFINES_MODULE: YES

    # dependencies:
      # - package: Kingfisher # nurirppan: tidak di pakai karna tidak ada thirdparty untuk UI

    dependencies:
      # Internal Dependencies
      - target: XAuthUIKit
      - target: XAuthCommunicationsKit
    
    sources:
      - path: Sources
        group: Modules/XAuthKit
      - path: Resources
        group: Modules/XAuthKit
      - path: Configs/Info.plist
        group: Modules/XAuthKit
      - path: Configs/xcconfig
        group: App/Configs

    configFiles:
      Staging Debug: Configs/xcconfig/Common.xcconfig
      Staging Release: Configs/xcconfig/Common.xcconfig
      Production Debug: Configs/xcconfig/Common.xcconfig
      Production Release: Configs/xcconfig/Common.xcconfig
  ############
  # Tests
  ############