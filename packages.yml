
# In this file we include all the package that we will use in our project.
# Remember that it doesn't matter if this file will be local (if you want to use Carthage)
# our using SwiftPackageManager

packages:

  Firebase:
    url: https://github.com/firebase/firebase-ios-sdk.git
    minorVersion: 12.6.0


# https://github.com/SwiftGen/SwiftGen