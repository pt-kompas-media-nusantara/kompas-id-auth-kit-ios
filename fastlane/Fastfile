# This file contains the fastlane.tools configuration
# You can find the documentation at https://docs.fastlane.tools
#
# For a list of all available actions, check out
#
#     https://docs.fastlane.tools/actions
#
# For a list of all available plugins, check out
#
#     https://docs.fastlane.tools/plugins/available-plugins
#

# Uncomment the line if you want fastlane to automatically update itself
# update_fastlane

Dir["#{File.dirname(__FILE__)}/*.rb"].each do |file|
  import file
end

# if File.exist?(".env.secret")
#   Dotenv.load(".env.secret")
# end

# Gunakan .overload, BUKAN .load
if File.exist?(".env.secret")
  # "overload" artinya: Timpa nilai yang sudah ada di memori dengan yang ada di file ini
  Dotenv.overload(".env.secret") 
end

default_platform(:ios)

platform :ios do

# ==================================variable env==================================== # 
APPLE_APPLICATION_SPECIFIC_PASSWORD = ENV["APPLE_APPLICATION_SPECIFIC_PASSWORD"]
APPLE_ISSUER_ID = ENV["APPLE_ISSUER_ID"]
APPLE_KEY_P8 = ENV["APPLE_KEY_P8"]
APPLE_KEY_P8_BASE_64 = ENV["APPLE_KEY_P8_BASE_64"]
APPLE_KEY_ID = ENV["APPLE_KEY_ID"]
APP_STORE_CONNECT_TEAM_ID = ENV["APP_STORE_CONNECT_TEAM_ID"]
DEVELOPER_PORTAL_TEAM_ID = ENV["DEVELOPER_PORTAL_TEAM_ID"]
FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD = ENV["FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD"]

# FASTLANE (SECRETS)
FASTLANE_APPLE_ID = ENV["FASTLANE_APPLE_ID"]
MATCH_PASSWORD = ENV["MATCH_PASSWORD"]
TEMP_KEYCHAIN_PASSWORD = ENV["TEMP_KEYCHAIN_PASSWORD"]
TEMP_KEYCHAIN_USER = ENV["TEMP_KEYCHAIN_USER"]

# GITHUB (SECRETS)
GIT_AUTHORIZATION = ENV["GIT_AUTHORIZATION"]
GITHUB_API_TOKEN = ENV["GITHUB_API_TOKEN"]

# SYSTEM PROJECT
TARGET_BY = ENV["TARGET_BY"]
TYPE_MATCH_APPSTORE = ENV["TYPE_MATCH_APPSTORE"]

# FILES
XCODEPROJ_APP = ENV["XCODEPROJ_APP"]
WORKSPACE_APP = ENV["WORKSPACE_APP"]

# DEPLOYMENT TYPES
PRODUCTION_BY_DELIVER = ENV["PRODUCTION_BY_DELIVER"]
DEVELOPMENT_BY_PILOT = ENV["DEVELOPMENT_BY_PILOT"]
DEVELOPMENT_BY_TESTFLIGHT = ENV["DEVELOPMENT_BY_TESTFLIGHT"]
DEPLOYMENT_TYPES = ENV["DEPLOYMENT_TYPES"]

# RELEASE
DEVELOPER_APP_ID = ENV["DEVELOPER_APP_ID"]
DEVELOPER_APP_IDENTIFIER = ENV["DEVELOPER_APP_IDENTIFIER"]
PROVISIONING_PROFILE_SPECIFIER_APPSTORE = ENV["PROVISIONING_PROFILE_SPECIFIER_APPSTORE"]
DEVELOPER_APP_IDENTIFIERS_DEFAULT = ENV["DEVELOPER_APP_IDENTIFIERS_DEFAULT"]

STAGING_DEBUG_SCHEME = ENV["STAGING_DEBUG_SCHEME"]
STAGING_RELEASE_SCHEME = ENV["STAGING_RELEASE_SCHEME"]
PRODUCTION_DEBUG_SCHEME = ENV["PRODUCTION_DEBUG_SCHEME"]
PRODUCTION_RELEASE_SCHEME = ENV["PRODUCTION_RELEASE_SCHEME"]

STAGING_DEBUG_CONFIGURATION = ENV["STAGING_DEBUG_CONFIGURATION"]
STAGING_RELEASE_CONFIGURATION = ENV["STAGING_RELEASE_CONFIGURATION"]
PRODUCTION_DEBUG_CONFIGURATION = ENV["PRODUCTION_DEBUG_CONFIGURATION"]
PRODUCTION_RELEASE_CONFIGURATION = ENV["PRODUCTION_RELEASE_CONFIGURATION"]

CONFIGURATIONS = ENV["CONFIGURATIONS"]
CONFIGURATION_GROUPS = ENV["CONFIGURATION_GROUPS"]

GITHUB_CONFIGURATION = ENV["GITHUB_CONFIGURATION"] 
GITHUB_DEPLOYMENT_TYPE = ENV["GITHUB_DEPLOYMENT_TYPE"] 
GITHUB_BUILD_NUMBER = ENV["GITHUB_BUILD_NUMBER"] 
GITHUB_VERSION_NUMBER = ENV["GITHUB_VERSION_NUMBER"]
GITHUB_NOTE = ENV["GITHUB_NOTE"]

end
